apiVersion: v1
kind: Service
metadata:
  name: app-html-lb
spec:
  selector:
    app: app-html
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer


# apiVersion: v1
# kind: Service
# metadata:
#   name: app-html-lb
# spec:
#   selector:
#     app.kubernetes.io/name: app-html
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 9376
#   clusterIP: 10.0.171.239
#   type: LoadBalancer
# status:
#   loadBalancer:
#     ingress:
#     - ip: 192.0.2.127